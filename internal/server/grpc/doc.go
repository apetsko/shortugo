// Package grpc provides a gRPC server setup and routing logic for the Shortugo URL shortening service.
// It exposes all core handler functionality via gRPC and reuses the same business logic as the HTTP layer.
package grpc
